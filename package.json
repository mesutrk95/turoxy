{
  "name": "ssh-tunnel-proxy-client",
  "version": "1.0.0",
  "description": "ssh tunnel proxy application",
  "authors": "rapiddev",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron-forge start",
    "package": "electron-forge package",
    "make": "npm run build --prefix ui && electron-forge make",
    "dev": "concurrently -k \"cd ui && npm start\" \"npm:electron\"",
    "electron": "wait-on tcp:25489 && electron .",
    "electron-dev": "electron ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron-forge/cli": "^6.0.4",
    "@electron-forge/maker-deb": "^6.0.4",
    "@electron-forge/maker-rpm": "^6.0.4",
    "@electron-forge/maker-squirrel": "^6.0.4",
    "@electron-forge/maker-zip": "^6.0.4",
    "concurrently": "^7.6.0",
    "electron": "^22.0.0",
    "electron-is-dev": "^2.0.0",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "global-tunnel-ng": "^2.7.1",
    "node-global-proxy": "^1.0.1",
    "node-ssh-forward": "^0.7.2",
    "regedit": "=5.0.0",
    "socksv5": "^0.0.4",
    "ssh2": "^1.11.0"
  }
}
